# 🚀 My Airflow Pipeline (Astro CLI + Papermill)

โปรเจกต์นี้เป็นระบบ Data Pipeline ที่รันบน **Apache Airflow** โดยใช้ **Astro CLI** เพื่อจัดการ Container และใช้ **Papermill** ในการสั่งรัน Jupyter Notebooks

## 🛠️ เครื่องมือที่ใช้
* **Astro CLI:** สำหรับรัน Airflow บน Docker แบบง่าย
* **Apache Airflow:** ตัวจัดการ Workflow (Orchestrator)
* **Papermill:** สำหรับสั่งรันและส่ง Parameter เข้าไปใน Jupyter Notebook (`.ipynb`)

## 📂 โครงสร้างโปรเจกต์
* `dags/`: เก็บไฟล์ Python สำหรับนิยาม Workflow (DAGs)
* `include/`: เก็บไฟล์ Jupyter Notebooks (.ipynb) ที่ต้องการรัน
* `requirements.txt`: รายการ Python libraries ที่ต้องใช้ (รวมถึง papermill)

## 🚀 วิธีการรันโปรเจกต์ (Local Development)

1. **เตรียมความพร้อม:** ตรวจสอบว่าเปิด Docker Desktop อยู่
2. **เริ่มทำงาน:** รันคำสั่งต่อไปนี้ใน Terminal:
astro dev start

เข้าใช้งานหน้าจอควบคุม: ไปที่ http://localhost:8080

User: admin
Password: admin

📝 วิธีเพิ่ม Notebook ใหม่เข้า Pipeline
นำไฟล์ .ipynb ไปวางที่โฟลเดอร์ include/

ตั้ง Tag ให้ Cell แรกใน Notebook เป็น parameters (เพื่อให้ Papermill ส่งค่าเข้าไปได้)

สร้าง DAG ในโฟลเดอร์ dags/ โดยใช้ PapermillOperator

หากต้องการหยุดรันและปิด Container ทั้งหมด:
astro dev stop

เช็ค apache-airflow-providers-postgres
astro dev bash --scheduler
pip list | grep postgres
