🚀 Astro Airflow Pipeline: Jupyter Notebook & Postgres Logging
โปรเจกต์ Data Pipeline ระดับมืออาชีพที่ผสานพลังของ Airflow, Papermill, และ Postgres เข้าด้วยกัน เพื่อรันระบบวิเคราะห์ข้อมูลอัตโนมัติแบบ Parameterized

🛠️ Tech Stack & Prerequisites
Astro CLI: ตัวจัดการ Environment ผ่าน Docker (Airflow 2.10+)

Papermill: ตัวขับเคลื่อนการรัน Notebook และการฉีดตัวแปร (Injection)

PostgreSQL: ระบบฐานข้อมูลสำหรับจัดเก็บ Metadata และ Run Logs

Docker Desktop: จำเป็นต้องรันอยู่เบื้องหลัง

📂 Project Structure
├── dags/
│   └── notebook_to_postgres.py   # ไฟล์นิยาม Workflow (DAG)
├── include/
│   ├── analysis.ipynb            # Notebook ต้นฉบับสำหรับวิเคราะห์
│   └── out-*.ipynb               # Notebook ผลลัพธ์ที่รันสำเร็จแล้ว (Auto-gen)
├── Dockerfile                    # บังคับติดตั้ง Provider (Postgres, Papermill)
└── requirements.txt              # รายการ Python Libraries
🚀 Getting Started
1. การเริ่มระบบครั้งแรก
ใช้คำสั่งด้านล่างเพื่อ Build และเริ่มทำงาน (เพิ่ม --wait เพื่อป้องกัน Timeout ช่วงลง Library):
astro dev start --wait 5m
Gemini said
ผมปรับปรุง README ให้ดูเป็นมืออาชีพมากขึ้น โดยจัดหมวดหมู่ให้ชัดเจน เพิ่มส่วน Architecture Diagram และเทคนิคการ Debug ที่เราเจอมา เพื่อให้ใครก็ตามที่มาอ่าน (หรือแม้แต่ตัวคุณเองในอนาคต) เข้าใจและเริ่มงานได้ทันทีครับ

🚀 Astro Airflow Pipeline: Jupyter Notebook & Postgres Logging
โปรเจกต์ Data Pipeline ระดับมืออาชีพที่ผสานพลังของ Airflow, Papermill, และ Postgres เข้าด้วยกัน เพื่อรันระบบวิเคราะห์ข้อมูลอัตโนมัติแบบ Parameterized

🛠️ Tech Stack & Prerequisites
Astro CLI: ตัวจัดการ Environment ผ่าน Docker (Airflow 2.10+)

Papermill: ตัวขับเคลื่อนการรัน Notebook และการฉีดตัวแปร (Injection)

PostgreSQL: ระบบฐานข้อมูลสำหรับจัดเก็บ Metadata และ Run Logs

Docker Desktop: จำเป็นต้องรันอยู่เบื้องหลัง

📂 Project Structure
Plaintext
├── dags/
│   └── notebook_to_postgres.py   # ไฟล์นิยาม Workflow (DAG)
├── include/
│   ├── analysis.ipynb            # Notebook ต้นฉบับสำหรับวิเคราะห์
│   └── out-*.ipynb               # Notebook ผลลัพธ์ที่รันสำเร็จแล้ว (Auto-gen)
├── Dockerfile                    # บังคับติดตั้ง Provider (Postgres, Papermill)
└── requirements.txt              # รายการ Python Libraries
🚀 Getting Started
1. การเริ่มระบบครั้งแรก
ใช้คำสั่งด้านล่างเพื่อ Build และเริ่มทำงาน (เพิ่ม --wait เพื่อป้องกัน Timeout ช่วงลง Library):

PowerShell
astro dev start --wait 5m
Airflow Webserver: http://localhost:8080 (admin/admin)

Postgres Internal Port: 5432 (สำหรับ Airflow)

Postgres External Port: 5435 (สำหรับ DBeaver/Datagrip)

2. การตั้งค่า Database Connection
ต้องสร้าง "ท่อเชื่อมต่อ" ในหน้าเว็บ Airflow (Admin > Connections) ดังนี้:
| Field | Value |
| :--- | :--- |
| Connection Id | my_postgres_db |
| Connection Type | Postgres |
| Host | postgres |
| Schema/Login/Pass | postgres |
| Port | 5432 |
📝 Workflow: การใช้งาน Notebook ใน Pipeline
สเต็ป 1: เตรียม Notebook ให้รับตัวแปร
เปิด Notebook ใน VS Code

ไปที่ Cell แรก -> คลิกปุ่ม Gear หรือ ... -> เลือก Add Tag

พิมพ์คำว่า parameters (ตัวพิมพ์เล็กทั้งหมด) แล้วกด Enter และ Save

สเต็ป 2: รันและตรวจสอบผล
เมื่อสั่ง Trigger DAG ระบบจะทำหน้าที่ดังนี้:

Papermill: อ่าน analysis.ipynb -> ฉีดวันที่รัน (ds) เข้าไป -> สร้างไฟล์ใหม่ใน include/out-YYYY-MM-DD.ipynb

Postgres: บันทึกเวลาและสถานะลงตาราง run_logs อัตโนมัติ
🔧 Troubleshooting (บันทึกการแก้ปัญหา)
Error: ModuleNotFoundError: หากเจอตัวนี้ ให้เช็คใน Dockerfile ว่ามีคำสั่ง RUN pip install หรือไม่ จากนั้นสั่ง astro dev stop และ start ใหม่

Error: Broken DAG: ตรวจสอบการสะกดชื่อตัวแปรใน Python (เช่น run_nb >> log_to_db) ต้องตรงกันเป๊ะ

Error: Connection Refused: ในหน้า Airflow UI ห้ามใช้ Port 5435 ให้ใช้พอร์ตภายในคือ 5432 เท่านั้น

Hard Reset: หากระบบค้างหรือฐานข้อมูลเพี้ยน ให้ใช้คำสั่งล้างไพ่:
astro dev kill
astro dev start

## 📊 SQL Quick Reference
คุณสามารถตรวจสอบผลลัพธ์การรัน Pipeline ได้ผ่าน Database Tool (เช่น DBeaver) โดยเชื่อมต่อผ่าน Port `5435`

### 1. ตรวจสอบข้อมูลการรันทั้งหมด
```sql
SELECT * FROM run_logs 
ORDER BY run_date DESC;
### 2. ดูสถิติการรันรายวัน
SELECT run_date, COUNT(*) as total_runs 
FROM run_logs 
GROUP BY run_date;
### 3. ล้างข้อมูล Log เก่า (ถ้าต้องการ)
TRUNCATE TABLE run_logs;

## ⌨️ Astro CLI Cheat Sheet
รวมคำสั่งที่ใช้บ่อยในการพัฒนาและจัดการ Lifecycle ของโปรเจกต์

### 🟢 การจัดการระบบ (Development)
* `astro dev start` : เริ่มรัน Airflow ใน Docker (รวมถึงการติดตั้ง library ใหม่ใน Dockerfile)
* `astro dev stop` : หยุดการทำงานของ Container ทั้งหมด
* `astro dev restart` : เริ่มระบบใหม่ (มักใช้หลังจากแก้ไขค่าคอนฟิกบางอย่าง)
* `astro dev kill` : ล้าง Container และ Metadata Database ทั้งหมด (ใช้เมื่อต้องการ Reset ระบบใหม่)

### 🔍 การตรวจสอบและ Debug
* `astro dev ps` : ตรวจสอบสถานะของ Container ทั้งหมด (Scheduler, Webserver, Triggerer, Postgres)
* `astro dev logs --scheduler` : ดู Log ของตัว Scheduler (ใช้เช็คเวลา DAG ไม่โผล่)
* `astro dev bash --scheduler` : มุดเข้าไปในเครื่อง Scheduler เพื่อรันคำสั่ง Linux หรือเช็คไฟล์

### 📦 การจัดการ Library & Providers
* `astro dev bash --scheduler -c "pip list"` : เช็ครายการ Python packages ที่ติดตั้งอยู่ในปัจจุบัน
* `astro dev bash --scheduler -c "airflow providers list"` : เช็ครายการ Providers ที่ติดตั้งแล้ว (เช่น Postgres, Amazon, Google)

### 🛠️ การจัดการ DAGs
* `astro dev parse` : ตรวจสอบ Syntax ของ DAGs ทั้งหมดว่าถูกต้องหรือไม่ก่อนสั่งรันจริง
